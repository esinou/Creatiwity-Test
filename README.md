# Creatiwity Test - Erwan SINOU

## Purpose

This application is a dynamic book in which the user can add pages composed with either plain text or an image described with a URL.

## Install & Launch

```bash
npm install && npm start
```